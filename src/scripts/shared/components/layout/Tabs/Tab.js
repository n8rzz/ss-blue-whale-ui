/**
 * @function Tab
 * @param {Object} props
 * @return {JSX}
 */
const Tab = props => {
    return (
        props.children
    );
};

/**
 * @property displayName
 * @type {String}
 * @static
 */
Tab.displayName = 'Tab';

export default Tab;
